<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="widget_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            Nothing OS home screen widget provider.

            Data flow (Flutter → widget):
              1.  Flutter calls HomeWidget.saveWidgetData() → SharedPreferences.
              2.  Flutter calls HomeWidget.updateWidget()   → triggers onUpdate().
              3.  NothingWidgetProvider.onUpdate() reads prefs via
                  HomeWidgetPlugin.getData() and applies them to RemoteViews.

            Data flow (widget → Flutter, background):
              1.  User taps the "+" button.
              2.  HomeWidgetBackgroundIntent fires a broadcast.
              3.  home_widget wakes a background Dart isolate and calls
                  widgetBackgroundCallback() defined in home_widget_service.dart.
        -->
        <receiver
            android:name=".NothingWidgetProvider"
            android:exported="true"
            android:label="Widget App">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nothing_widget_info" />
        </receiver>

        <!--
            Required by home_widget ^0.9.0 for background interactivity callbacks.
            Handles the broadcast fired by HomeWidgetBackgroundIntent (e.g. the
            "+" button on the home screen widget) and wakes the Dart isolate to
            run widgetBackgroundCallback() without opening the app.
        -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
